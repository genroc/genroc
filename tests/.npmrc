# openapi-typescript still peer-requires typescript ^5.x, and this project is on 6. The tool
# only reads openapi.json to emit generated/api.ts — it never typechecks this project — so
# the conflict is inert. Remove this the release its peer range widens.
legacy-peer-deps=true

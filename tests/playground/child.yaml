# yaml-language-server: $schema=https://genroc.org/process-schema.json
name: child
tasks:
  - id: fourth
    output:
      test: "$: true"
    switch:
      - case: "true"
        raise:
          code: "fourth_failed"
          message: "Fourth task failed"
      - goto: end
output: "$: outputs.fourth"
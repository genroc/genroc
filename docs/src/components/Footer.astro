---
import { url } from '../lib/url'
---

<footer id="page-end">
  <span>genroc — a durable process orchestrator</span>
  <a href="https://github.com/genroc/genroc">source</a>
  {/* Same origin in production, so prefetchAll would pull it in; the benchmark app is a
      separate artifact the deploy only preserves. */}
  <a href="https://genroc.org/bench/" data-astro-prefetch="false">benchmarks</a>
  <a href={url('/')}>docs home</a>
</footer>

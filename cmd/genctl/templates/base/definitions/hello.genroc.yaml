name: hello
input_schema:
  type: object
  properties:
    who: { type: string, default: "world" }
tasks:
  - id: greet
    output:
      greeting: "hello, ${input.who}"
    switch:
      - goto: end

output: "$: outputs.greet"
